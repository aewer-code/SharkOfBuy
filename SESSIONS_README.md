# Управление Telegram сессиями для рассылки

Этот функционал позволяет загружать Telegram сессии и рассылать сообщения по чатам от имени этих аккаунтов.

## Установка зависимостей

```bash
pip install -r requirements.txt
```

## Как использовать

### 1. Получение API ID и API Hash

1. Перейдите на https://my.telegram.org/apps
2. Войдите в свой аккаунт Telegram
3. Создайте новое приложение
4. Скопируйте `api_id` и `api_hash`

### 2. Создание сессии

Для создания сессии вам понадобится файл `.session`. Вы можете создать его с помощью Telethon:

```python
from telethon import TelegramClient

api_id = YOUR_API_ID
api_hash = 'YOUR_API_HASH'

client = TelegramClient('session_name', api_id, api_hash)
client.start()
# После этого файл session_name.session будет создан
```

Или используйте готовый файл сессии, если он у вас есть.

### 3. Добавление сессии в бот

1. Отправьте боту команду `/sessions`
2. Нажмите "➕ Добавить сессию"
3. Введите имя сессии (например: `my_account`)
4. Введите API ID (число)
5. Введите API Hash (строка)
6. Отправьте файл `.session`

### 4. Сканирование чатов

1. В меню `/sessions` выберите "📋 Список чатов"
2. Выберите сессию
3. Бот просканирует все чаты этой сессии (до 200 чатов)

### 5. Рассылка сообщений

1. В меню `/sessions` выберите "📤 Рассылка"
2. Выберите сессию
3. Выберите чаты для рассылки (можно выбрать несколько или все)
4. Нажмите "📤 Начать рассылку"
5. Введите текст сообщения
6. Бот отправит сообщение во все выбранные чаты

**Важно:** Сообщения отправляются как новые (не пересылаются), поэтому получатели не увидят, что сообщение было переслано.

## Безопасность

- Файлы сессий хранятся в папке `sessions/`
- Данные о сессиях (API ID/Hash) хранятся в `sessions/sessions_data.json`
- Убедитесь, что эти файлы не попадут в публичный доступ
- Добавьте `sessions/` в `.gitignore`

## Ограничения

- Задержка между отправками: 1 секунда (настраивается)
- При FloodWait бот автоматически ждет и повторяет отправку
- Максимум 200 чатов при сканировании (можно увеличить в коде)

## Команды

- `/sessions` - Управление сессиями (только для админов)

## Структура файлов

```
sessions/
├── session_name.session    # Файлы сессий
└── sessions_data.json      # Метаданные сессий
```

